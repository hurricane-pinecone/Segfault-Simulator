
// TODO: Make a gui that can create levels using a sprite sheet.
// IE, it should be able to tile a given region, and assign sprites to each
// tile, then export to a .levelmap file

#include <array>
class MapEditor
{
  //   public:
  //     static std::array<std::, size_t Size>
}
