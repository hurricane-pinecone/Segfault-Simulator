# Runtime tests link the full engine (SDL/OpenGL/imgui). None yet -- add them as:
#
#   add_executable(fooTests foo/fooTests.cpp)
#   target_link_libraries(fooTests PRIVATE engine)
#   add_test(NAME fooTests COMMAND fooTests)
