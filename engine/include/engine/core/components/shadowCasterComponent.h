#pragma once

namespace sfs
{

/**
 * Tag: the entity casts a projected sprite shadow on the isometric path.
 * Presence alone opts the sprite in -- it carries no data.
 */
struct ShadowCasterComponent
{
};

} // namespace sfs
