#pragma once

namespace sfs
{

/**
 * Tag: marks an entity as a cell of the isometric tile grid, letting systems
 * pick out grid tiles from other entities. Carries no data.
 */
struct IsometricTile
{
};

} // namespace sfs
